// Setting the scores and selecting HTML elements
var userScore = 0;
var computerScore = 0;
const pScore = document.getElementById('userScore');
const cScore = document.getElementById('computerScore');
const buttons = document.querySeletorAll('');